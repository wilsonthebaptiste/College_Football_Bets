{
  "name": "@cfb/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "bundle": "wrangler deploy --dry-run --env production --outdir dist",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@cfb/shared": "*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
